const chalk = require('chalk');

module.exports = {
    bold : chalk.bold,
    connected : chalk.rgb(0, 150, 0),
    error : chalk.rgb(200, 0, 0),
    disconnected : chalk.rgb(200, 200, 0),
    termination : chalk.rgb(255, 255, 255),
}